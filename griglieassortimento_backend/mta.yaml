_schema-version: 3.3.0
ID: griglieassortimento_backend
version: 1.0.0
description: "A simple CAP project."
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: griglieassortimento_backend-srv
    type: nodejs
    path: gen/srv
    parameters:
      buildpack: nodejs_buildpack
      readiness-health-check-type: http
      readiness-health-check-http-endpoint: /health
    build-parameters:
      builder: npm
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: griglieassortimento_backend-auth
      - name: griglieassortimento_backend-db
      - name: griglieassortimento_backend-destination
      - name: griglieassortimento_backend-autoscaler
        parameters:
          path: ./autoscaler.json

  - name: griglieassortimento_backend-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: griglieassortimento_backend-db

  - name: griglieassortimento_backend-destination-content
    type: com.sap.application.content
    requires:
      - name: griglieassortimento_backend-destination
        parameters:
          content-target: true
      - name: griglieassortimento_backend-auth
        parameters:
          service-key:
            name: uaa_griglieassortimento-key
    parameters:
      content:
        instance:
          destinations:
            - Authentication: OAuth2UserTokenExchange
              Name: uaa_griglieassortimento
              ServiceInstanceName: griglieassortimento_backend-auth
              ServiceKeyName: uaa_griglieassortimento-key
              sap.cloud.service: griglieassortimento
          existing_destinations_policy: ignore
    build-parameters:
      no-source: true
resources:
  - name: griglieassortimento_backend-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: griglieassortimento_backend
        tenant-mode: dedicated
  - name: griglieassortimento_backend-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
  - name: griglieassortimento_backend-destination
    type: org.cloudfoundry.managed-service
    parameters:
      service: destination
      service-plan: lite
  - name: griglieassortimento_backend-autoscaler
    type: org.cloudfoundry.managed-service
    parameters:
      service: autoscaler
      service-plan: standard
